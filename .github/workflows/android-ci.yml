name: Android-CI

on:
  push:
    branches: 
      - master
      
jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        
        steps:
            - name: Checkout
              uses: actions/checkout@master
              with:
                ref: master
                
            - name: set up JDK 1.8
              uses: actions/setup-java@v1
              with:
                java-version: 1.8
              
            - name: Make Gradle executable
              run: chmod +x ./gradlew
              
            - name: Pull Submodules
              run: git submodule update --init --recursive --remote
                
            - name: Download Go Libs
              run: curl -L https://github.com/trojan-gfw/igniter-go-libs/releases/download/1.10/golibs.aar > app/src/libs/golibs.aar
                
            - name: Download Dependencies
              run: ./gradlew androidDependencies

            - name: Run Tests
              run: ./gradlew lint test

            - name: Build with Gradle
              run: ./gradlew build

            - name: Initial build
              run: ./gradlew clean assembleRelease --no-daemon --stacktrace

            - name: Deploy Igniter
              run: ./gradlew crashlyticsUploadDistributionProductionRelease --stacktrace --debug --no-daemon
